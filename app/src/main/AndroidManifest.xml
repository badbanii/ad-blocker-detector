<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <package android:name="com.savageorgiev.blockthis" />
        <package android:name="org.adaway" />
        <package android:name="org.blokada.alarm"/>
        <package android:name="org.blokada.origin.alarm" />
        <package android:name="org.blokada.plus" />
        <package android:name="org.dschuermann.adaway" />
        <package android:name="com.github.adblockremover" />
        <package android:name="com.stevesoltys.seedvault.plugins.adblocker" />
        <package android:name="com.industries.tracer.apprtc" />
        <package android:name= "com.blokada.lite" />
        <package android:name=  "com.blokada.alarm.lite" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdBlockerDetector"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AdBlockerDetector">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>